<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Projects</title>
  <link rel="stylesheet" href="/css/main.css" />
</head>
<body>
  <%- include('partials/navbar', {page:"/solutions/projects"}) %>
  <div class="container mx-auto my-8">
    <div class="hero bg-base-200 rounded-lg p-6">
      <div class="hero-content text-center">
        <div class="max-w-md">
          <h1 class="text-5xl font-bold">Projects</h1>
          <p class="py-6">Browse through our climate solution projects.</p>
          <div class="flex justify-center space-x-4">
            <a href="/solutions/projects?sector=Industry" class="btn btn-primary">Industry</a>
            <a href="/solutions/projects?sector=Transportation" class="btn btn-primary">Transportation</a>
            <a href="/solutions/projects?sector=Electricity" class="btn btn-primary">Electricity</a>
          </div>
        </div>
      </div>
    </div>
    <table class="table-auto w-full mt-4 border-collapse border border-gray-400">
      <tbody>
        <% projects.forEach(project => { %>
          <tr>
            <td class="border border-gray-300 p-2"><%= project.title %></td>
            <td class="border border-gray-300 p-2">
              <a href="/solutions/projects?sector=<%= project.sector %>" class="btn"><%= project.sector %></a>
            </td>
            <td class="border border-gray-300 p-2"><%= project.summary_short %></td>
            <td class="border border-gray-300 p-2">
              <a href="/solutions/projects/<%= project.id %>" class="btn btn-primary">Details</a>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
</body>
</html>
